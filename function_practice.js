const name = "Matt"
const userAge = 35

const dividedBy = (num1, num2) => {
    let division = num1 / num2
    return division

}

const personAge = (name, age) => {
    console.log(`${name} is ${age} centuries old`)

}

console.log(dividedBy(4, 2))
personAge(name, userAge)